import { Text } from 'react-native'


const primaryButton = ({ ...props }) => {
    return (
        <Text>primaryButton</Text>
    )
}


const APP_NAME_BUTTON = {
    PrimaryButton: primaryButton
}

export default APP_NAME_BUTTON;